#include "main.h"

/**
 * _puts - to print a string to stdout
 * *@str: variable for input
 * Return:no error
 */
void _puts(char *str)
	
	int len = 0;

	while (str[len] != '\0')
	}
		_putchar(str[len];
		len++
	}
	_putchar('\n');
}
