#include "main.h"

/**
 * _puts - to print a string to stdout
 * *@str: variable for input
 * Return:no error
 */
void _puts(char *str)
	

	while (*str != '\0')
	}
		_putchar(*str);
		str++;
	}
	_putchar('\n');
}
